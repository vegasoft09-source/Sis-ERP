@{
    ViewData["Title"] = "Reportes";
    ViewData["Breadcrumbs"] = new[] {
        ("Dashboard", "/"),
        ("Reportes", "/Reportes")
    };
}

<div class="cl-top">
    <div>
        <h1 class="cl-title">Reportes</h1>
        <p class="cl-subtitle">Módulo en construcción.</p>
    </div>
</div>

<div class="cl-card">
    <div style="padding: 40px; text-align: center; color: var(--text-muted);">
        <svg viewBox="0 0 24 24" style="width: 48px; height: 48px; stroke: currentColor; fill: none; margin-bottom: 16px;">
            <circle cx="12" cy="12" r="10" />
            <line x1="12" y1="8" x2="12" y2="12" />
            <line x1="12" y1="16" x2="12.01" y2="16" />
        </svg>
        <h3>Módulo de Reportes</h3>
        <p>Aún no se han especificado los requerimientos para este apartado.</p>
    </div>
</div>
