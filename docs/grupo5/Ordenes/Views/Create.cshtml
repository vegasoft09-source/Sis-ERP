@model Hevelab2026.Models.OrdenVenta

@{
    ViewData["Title"] = "Nueva Orden";
    ViewData["Breadcrumbs"] = new[] {
        ("Dashboard", "/"),
        ("Ventas", "#"),
        ("Órdenes", "/Ordenes")
    };
}

@section Styles {
    <link rel="stylesheet" href="~/css/ordenes.css" />
}

<div class="ordenes-container">

    <div class="ordenes-header">
        <div>
            <h1>Nueva Orden</h1>
            <p>Registra una nueva orden de venta.</p>
        </div>

        <a href="/Ordenes" class="btn-secundario">
            Volver
        </a>
    </div>

    <form asp-action="Create" method="post" class="form-card">

        <div class="form-grid">

            <div class="form-grupo">
                <label>Número de pedido</label>
                <input asp-for="NumeroPedido" placeholder="Ejemplo: S0015" required />
            </div>

            <div class="form-grupo">
                <label>Fecha de creación</label>
                <input asp-for="FechaCreacion" placeholder="Ejemplo: 25/10/2023" required />
            </div>

            <div class="form-grupo">
                <label>Cliente</label>
                <input asp-for="Cliente" placeholder="Nombre del cliente" required />
            </div>

            <div class="form-grupo">
                <label>Razón Social</label>
                <input asp-for="RazonSocial" placeholder="Empresa o razón social" required />
            </div>

            <div class="form-grupo">
                <label>RUC</label>
                <input asp-for="Ruc" placeholder="RUC" required />
            </div>

            <div class="form-grupo">
                <label>Subtotal</label>
                <input asp-for="Subtotal" type="number" step="0.01" placeholder="0.00" required />
            </div>

            <div class="form-grupo">
                <label>Total</label>
                <input asp-for="Total" type="number" step="0.01" placeholder="0.00" required />
            </div>

            <div class="form-grupo">
                <label>Estado</label>
                <select asp-for="Estado" required>
                    <option value="BORRADOR">BORRADOR</option>
                    <option value="POR FACTURAR">POR FACTURAR</option>
                    <option value="FACTURADO POR COMPLETO">FACTURADO POR COMPLETO</option>
                </select>
            </div>

        </div>

        <div class="form-actions">
            <a href="/Ordenes" class="btn-secundario">
                Cancelar
            </a>

            <button type="submit" class="btn-nuevo">
                Guardar
            </button>
        </div>

    </form>

</div>